{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile && pnpm build:packages",
  "builds": [
    {
      "src": "apps/api/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/api/api/index.ts"
    }
  ]
}
